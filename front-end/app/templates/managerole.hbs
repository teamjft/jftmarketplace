<div class="col-md-12"><br/>
    <h1 class="text-center text-info"> || Manage User Role Page || </h1>
    <div class="container">
        <form {{action "addCategory" model.post on="submit" }}>
            <h3 class="w3-wide w3-opacity text-center">
                <span class="glyphicon glyphicon-thumbs-up"> </span> Manage Role Table ...
                <span class="glyphicon glyphicon-thumbs-up"> </span>
            </h3><br/> {{!--
            <div class="jumbotron">
                <div class="text-danger">
                    <ul>
                        {{#each model.post.msg as | ms |}}
                        <li>{{ms}}</li>
                        {{/each}}
                    </ul>
                </div>
                <div class="form-group">
                    <label for='name'>Category Name</label> {{input value=model.post.name id='name' class="form-control"}}
                </div>
                <div class="form-group">
                    <label for='description'>Category Description</label> {{input value=model.post.description id='description'
                    class="form-control"}}
                </div>
                <div class="form-group text-center">
                    <button class="btn btn-primary" type="submit">Save</button>
                    <button class="btn" type="button">Reset</button>
                </div>
            </div>--}}
        </form>
    </div>
    {{!--
    <h3 class="w3-wide w3-opacity text-center"> Category Table </h3> <br/>--}}
    <div class="container">
        <div class="col-md-12">
            {{#if model}}
            <table class="table table-striped table-bordered table-responsive">

                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Email</th>
                        <th>Contact</th>
                        <th>Change Role</th>
                        <th>Action</th>
                        {{!--
                        <th>Delete</th>--}}
                    </tr>
                </thead>

                <tbody>
                    {{#each model as | user |}}
                    <tr>
                        <td>{{user.fname}} {{user.lname}} {{user.uuid}}</td>
                        <td>{{user.email}}</td>
                        <td>{{user.phone}}</td>
                        <td>
                            <input class="form-group" type="checkbox" checked={{cbState}} />
                            <span> Admin </span>
                            <input type="checkbox" checked />
                            <span> User </span>
                        </td>
                        <td>{{#link-to "editcategory" category.id class="btn btn-primary btn" }}Update{{/link-to}}
                        
                        <a href="#" {{action "deleteUser" user.uuid}} class="btn btn-info btn">Delete</a></td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            {{else}}
            <div class="row text-danger text-center">
                <h3>-- No User Found --</h3>
            </div>
            {{/if}}
        </div>
    </div>
</div>
{{outlet}}